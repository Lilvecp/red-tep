# Bugfixes & Mejoras (Abril 16) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Corregir 7 bugs/mejoras: banner de empresa, filtro disponibilidad, estado postulado, candidatos en oferta, copiar email, rechazar empresa en admin, BOM en CSV, y bloque blanco en admin.

**Architecture:** Cambios frontend en React/JSX + un endpoint backend Express/Prisma nuevo. Sin tests formales (no existe test suite). Verificar manualmente en browser.

**Tech Stack:** React 18 + Vite, Node.js + Express + Prisma, PostgreSQL

---

## Files

| Acción | Archivo |
|--------|---------|
| Modify | `frontend/src/pages/company/PublicCompanyProfile.jsx` |
| Modify | `frontend/src/pages/worker/OfertasPage.jsx` |
| Modify | `frontend/src/pages/worker/OfertaDetalle.jsx` |
| Modify | `frontend/src/pages/company/MisOfertasPage.jsx` |
| Modify | `frontend/src/pages/company/CandidatosPage.jsx` |
| Modify | `frontend/src/pages/admin/AdminDashboard.jsx` |
| Modify | `backend/src/controllers/oferta.controller.js` |
| Modify | `backend/src/controllers/admin.controller.js` |
| Modify | `backend/src/routes/admin.routes.js` |
| Modify | `backend/src/routes/oferta.routes.js` |
| Modify | `frontend/src/services/index.js` |

---

### Task 1: Fix banner recorta logo en PublicCompanyProfile

**Files:**
- Modify: `frontend/src/pages/company/PublicCompanyProfile.jsx:98`

- [ ] **Step 1: Quitar `overflow:'hidden'` del wrapper exterior y mover borderRadius al banner**

En `PublicCompanyProfile.jsx` línea 98-99, cambiar:
```jsx
// ANTES
<div style={{ borderRadius: 16, overflow: 'hidden', marginBottom: '1.25rem', border: '1px solid var(--border)' }}>
  <div style={{ height: 140, background: banner, position: 'relative' }} />
```
```jsx
// DESPUÉS
<div style={{ borderRadius: 16, marginBottom: '1.25rem', border: '1px solid var(--border)' }}>
  <div style={{ height: 140, background: banner, position: 'relative', borderRadius: '16px 16px 0 0' }} />
```

- [ ] **Step 2: Commit**
```bash
git add frontend/src/pages/company/PublicCompanyProfile.jsx
git commit -m "fix: remove overflow hidden from company profile wrapper to prevent logo clipping"
```

---

### Task 2: Quitar filtro de disponibilidad en OfertasPage

**Files:**
- Modify: `frontend/src/pages/worker/OfertasPage.jsx`

- [ ] **Step 1: Eliminar select de disponibilidad y limpiar estado**

En `OfertasPage.jsx`:

```jsx
// Cambiar estado inicial (línea 12):
const [filters, setFilters] = useState({ especialidad: '' })

// Eliminar el <select> completo (líneas 30-34):
// BORRAR:
<select value={filters.disponibilidad} onChange={e=>setFilters({...filters,disponibilidad:e.target.value})}
  style={{ ... }}>
  <option value="">Disponibilidad</option>
  {['TIEMPO_COMPLETO','MEDIO_TIEMPO','FINES_DE_SEMANA'].map(d=><option key={d} value={d}>{d.replace(/_/g,' ')}</option>)}
</select>
```

- [ ] **Step 2: Commit**
```bash
git add frontend/src/pages/worker/OfertasPage.jsx
git commit -m "fix: remove disponibilidad filter from ofertas page, keep only especialidad"
```

---

### Task 3: Backend — saber si ya postulé + listar candidatos de oferta

**Files:**
- Modify: `backend/src/controllers/oferta.controller.js`
- Modify: `backend/src/routes/oferta.routes.js`

- [ ] **Step 1: Agregar `yaPostule` en `getById`**

En `oferta.controller.js`, función `getById`, después de obtener la oferta añadir:
```js
const getById = async (req, res) => {
  try {
    const oferta = await prisma.oferta.findUnique({
      where: { id: Number(req.params.id) },
      include: {
        company: true,
        postulaciones: { include: { worker: { include: { user: { select: { nombre: true } } } } } }
      }
    })
    if (!oferta) return res.status(404).json({ error: 'Oferta no encontrada' })

    // Detectar si el usuario logueado ya postuló
    let yaPostule = false
    if (req.user) {
      const worker = await prisma.worker.findUnique({ where: { userId: req.user.id } })
      if (worker) {
        const existing = await prisma.postulacion.findFirst({
          where: { workerId: worker.id, ofertaId: oferta.id }
        })
        yaPostule = !!existing
      }
    }

    res.json({ ...oferta, yaPostule })
  } catch (err) { console.error(err); res.status(500).json({ error: 'Error interno del servidor' }) }
}
```

- [ ] **Step 2: Agregar endpoint `GET /api/ofertas/:id/postulaciones`**

Al final de `oferta.controller.js`, antes del `module.exports`:
```js
// GET /api/ofertas/:id/postulaciones — solo la empresa dueña
const getPostulaciones = async (req, res) => {
  try {
    const company = await prisma.company.findUnique({ where: { userId: req.user.id } })
    const oferta  = await prisma.oferta.findFirst({ where: { id: Number(req.params.id), companyId: company?.id } })
    if (!oferta) return res.status(403).json({ error: 'No autorizado' })

    const postulaciones = await prisma.postulacion.findMany({
      where: { ofertaId: oferta.id },
      include: {
        worker: {
          include: {
            user: { select: { nombre: true, email: true } },
            habilidades: { select: { nombre: true } },
          }
        }
      },
      orderBy: { createdAt: 'desc' }
    })
    res.json(postulaciones)
  } catch (err) { console.error(err); res.status(500).json({ error: 'Error interno del servidor' }) }
}
```

Actualizar `module.exports`:
```js
module.exports = { list, getById, create, update, postular, remove, getPostulaciones }
```

- [ ] **Step 3: Registrar ruta en `oferta.routes.js`**

```js
// Agregar antes del module.exports o al final de las rutas existentes:
router.get('/:id/postulaciones', auth, requireRole('COMPANY'), ofertaController.getPostulaciones)
```

- [ ] **Step 4: Commit**
```bash
git add backend/src/controllers/oferta.controller.js backend/src/routes/oferta.routes.js
git commit -m "feat: add yaPostule to getById and getPostulaciones endpoint for company"
```

---

### Task 4: Frontend — botón "Ya postulado" + empresa ve candidatos

**Files:**
- Modify: `frontend/src/pages/worker/OfertaDetalle.jsx`
- Modify: `frontend/src/pages/company/MisOfertasPage.jsx`
- Modify: `frontend/src/services/index.js`

- [ ] **Step 1: Agregar servicio `getPostulaciones` en services/index.js**

En la sección `ofertaService`:
```js
export const ofertaService = {
  getAll:            (filters) => api.get('/ofertas', { params: filters }),
  getById:           (id)      => api.get(`/ofertas/${id}`),
  create:            (data)    => api.post('/ofertas', data),
  update:            (id, d)   => api.put(`/ofertas/${id}`, d),
  remove:            (id)      => api.delete(`/ofertas/${id}`),
  postular:          (id)      => api.post(`/ofertas/${id}/postular`),
  getPostulaciones:  (id)      => api.get(`/ofertas/${id}/postulaciones`),
}
```

- [ ] **Step 2: Mostrar "Ya postulado" en OfertaDetalle**

En `OfertaDetalle.jsx`, la oferta ya trae `yaPostule` del backend. Actualizar el estado y los botones:

```jsx
// Añadir estado yaPostule derivado de la oferta:
// Cambiar handlePostular para actualizar estado local:
const handlePostular = async () => {
  setPosting(true)
  try {
    await ofertaService.postular(id)
    setOferta(prev => ({ ...prev, yaPostule: true }))
    toast.success('¡Postulación enviada!')
  } catch (err) {
    toast.error(err.response?.data?.error || 'Error al postular')
  } finally { setPosting(false) }
}
```

Reemplazar los dos botones de postular (líneas 63 y 115-117) con lógica condicional:
```jsx
// Botón principal (header):
{oferta.yaPostule ? (
  <button disabled style={{ padding:'9px 18px', borderRadius:8, border:'none', background:'var(--surface2)', color:'var(--green-lit)', fontSize:'.85rem', fontWeight:600, cursor:'default', fontFamily:"'Figtree','DM Sans',sans-serif" }}>
    ✓ Ya postulado
  </button>
) : (
  <BtnGreen onClick={handlePostular} disabled={posting}>{posting ? 'Enviando...' : 'Postular Ahora'}</BtnGreen>
)}

// Botón sidebar (reemplazar BtnGreen del sidebar):
{oferta.yaPostule ? (
  <button disabled style={{ width:'100%', padding:12, fontSize:'.88rem', borderRadius:9, border:'none', background:'var(--surface2)', color:'var(--green-lit)', fontWeight:600, cursor:'default', fontFamily:"'Figtree','DM Sans',sans-serif" }}>
    ✓ Ya postulado a esta oferta
  </button>
) : (
  <BtnGreen onClick={handlePostular} disabled={posting} style={{ width:'100%', padding:12, fontSize:'.88rem' }}>
    {posting ? 'Enviando postulación...' : 'Postular a esta oferta'}
  </BtnGreen>
)}
```

- [ ] **Step 3: Modal de candidatos en MisOfertasPage**

En `MisOfertasPage.jsx` agregar estado y modal de candidatos:

```jsx
// Nuevo estado al inicio del componente:
const [candidatosModal, setCandidatosModal] = useState(null) // { ofertaId, cargo }
const [candidatos, setCandidatos] = useState([])
const [loadingCandidatos, setLoadingCandidatos] = useState(false)

const openCandidatos = async (o) => {
  setCandidatosModal({ ofertaId: o.id, cargo: o.cargo })
  setLoadingCandidatos(true)
  try {
    const res = await ofertaService.getPostulaciones(o.id)
    setCandidatos(res.data)
  } catch { toast.error('Error al cargar candidatos') }
  finally { setLoadingCandidatos(false) }
}
```

Agregar botón "Ver candidatos" en cada fila de oferta (después del Badge de activa):
```jsx
<button
  onClick={() => openCandidatos(o)}
  style={{ padding:'5px 12px', borderRadius:7, border:'1px solid var(--border2)', background:'var(--surface2)', color:'var(--text2)', fontSize:'.75rem', cursor:'pointer', fontFamily:"'Figtree',sans-serif" }}
>
  👥 {o.postulaciones?.length || 0} candidatos
</button>
```

Agregar modal de candidatos al final del componente (antes del cierre del `return`):
```jsx
{candidatosModal && (
  <div
    onClick={e => { if (e.target === e.currentTarget) setCandidatosModal(null) }}
    style={{ position:'fixed', inset:0, background:'rgba(0,0,0,.6)', backdropFilter:'blur(4px)', zIndex:200, display:'flex', alignItems:'center', justifyContent:'center', padding:'1rem' }}
  >
    <div style={{ background:'var(--bg2)', border:'1px solid var(--border)', borderRadius:16, padding:'1.5rem', width:'100%', maxWidth:540, maxHeight:'80vh', display:'flex', flexDirection:'column' }}>
      <div style={{ display:'flex', justifyContent:'space-between', alignItems:'center', marginBottom:'1.25rem' }}>
        <div style={{ fontFamily:"'Sora',sans-serif", fontWeight:700, fontSize:'1rem', color:'var(--text)' }}>
          Candidatos — {candidatosModal.cargo}
        </div>
        <button onClick={() => setCandidatosModal(null)} style={{ background:'none', border:'none', cursor:'pointer', color:'var(--text3)', fontSize:'1.2rem' }}>×</button>
      </div>
      <div style={{ overflowY:'auto', flex:1 }}>
        {loadingCandidatos ? (
          <div style={{ color:'var(--text2)', textAlign:'center', padding:'2rem' }}>Cargando...</div>
        ) : candidatos.length === 0 ? (
          <div style={{ color:'var(--text3)', textAlign:'center', padding:'2rem', fontSize:'.85rem' }}>Aún no hay postulantes.</div>
        ) : (
          <div style={{ display:'flex', flexDirection:'column', gap:8 }}>
            {candidatos.map(p => (
              <div key={p.id} style={{ background:'var(--surface)', border:'1px solid var(--border)', borderRadius:10, padding:'.9rem 1rem', display:'flex', alignItems:'center', gap:12 }}>
                <div style={{ width:38, height:38, borderRadius:10, background:'var(--green)', display:'flex', alignItems:'center', justifyContent:'center', fontFamily:"'Sora',sans-serif", fontWeight:700, fontSize:'.8rem', color:'#fff', flexShrink:0 }}>
                  {(p.worker?.user?.nombre || 'E').slice(0,2).toUpperCase()}
                </div>
                <div style={{ flex:1, minWidth:0 }}>
                  <div style={{ fontWeight:600, fontSize:'.85rem', color:'var(--text)', overflow:'hidden', textOverflow:'ellipsis', whiteSpace:'nowrap' }}>{p.worker?.user?.nombre}</div>
                  <div style={{ fontSize:'.7rem', color:'var(--text2)' }}>{p.worker?.especialidad || 'Sin especialidad'}</div>
                </div>
                <div style={{ display:'flex', gap:6, flexShrink:0 }}>
                  <button
                    onClick={() => window.open(`/trabajadores/${p.worker?.id}`, '_blank')}
                    style={{ padding:'5px 10px', borderRadius:7, border:'1px solid var(--border2)', background:'var(--surface2)', color:'var(--text2)', fontSize:'.75rem', cursor:'pointer' }}
                  >
                    Ver perfil
                  </button>
                  <button
                    onClick={() => {
                      navigator.clipboard.writeText(p.worker?.user?.email || '')
                      toast.success('Correo copiado al portapapeles')
                    }}
                    style={{ padding:'5px 10px', borderRadius:7, border:'none', background:'var(--green-mid)', color:'#fff', fontSize:'.75rem', cursor:'pointer' }}
                  >
                    Contactar
                  </button>
                </div>
              </div>
            ))}
          </div>
        )}
      </div>
    </div>
  </div>
)}
```

- [ ] **Step 4: Commit**
```bash
git add frontend/src/services/index.js frontend/src/pages/worker/OfertaDetalle.jsx frontend/src/pages/company/MisOfertasPage.jsx
git commit -m "feat: show ya-postulado state and add candidates modal to company offers"
```

---

### Task 5: Botón "Contactar" copia email en CandidatosPage

**Files:**
- Modify: `frontend/src/pages/company/CandidatosPage.jsx:207-219`

- [ ] **Step 1: Cambiar `<a>` por `<button>` con clipboard**

Reemplazar el bloque del botón Contactar en el preview sidebar (líneas ~207-219):
```jsx
// ANTES:
<a
  href={`mailto:${selected.user?.email}`}
  style={{ ... textDecoration: 'none', ... }}
>
  Contactar
</a>

// DESPUÉS:
<button
  onClick={() => {
    navigator.clipboard.writeText(selected.user?.email || '')
    toast.success('Correo copiado: ' + selected.user?.email)
  }}
  style={{
    flex: 1, padding: '7px', borderRadius: 8,
    border: '1px solid var(--border)',
    background: 'transparent', color: 'var(--text2)',
    fontSize: '.78rem', cursor: 'pointer', textAlign: 'center',
    fontFamily: "'Figtree','DM Sans',sans-serif",
  }}
>
  Contactar
</button>
```

Verificar que `toast` esté importado (`import { toast } from 'react-hot-toast'`).

- [ ] **Step 2: Commit**
```bash
git add frontend/src/pages/company/CandidatosPage.jsx
git commit -m "feat: contactar button copies email to clipboard instead of mailto"
```

---

### Task 6: Admin puede rechazar empresa

**Files:**
- Modify: `backend/src/controllers/admin.controller.js`
- Modify: `backend/src/routes/admin.routes.js`
- Modify: `frontend/src/pages/admin/AdminDashboard.jsx`
- Modify: `frontend/src/services/index.js`

- [ ] **Step 1: Agregar `rejectCompany` en admin.controller.js**

Después de `approveCompany`:
```js
const rejectCompany = async (req, res) => {
  try {
    const { feedback } = req.body
    const company = await prisma.company.update({
      where: { id: Number(req.params.id) },
      data: {
        verificationRequested: false,
        ...(feedback ? { adminFeedback: feedback } : {}),
      },
    })
    res.json({ message: 'Verificación rechazada', company })
  } catch (err) { console.error(err); res.status(500).json({ error: 'Error interno del servidor' }) }
}
```

Agregar al `module.exports`:
```js
module.exports = {
  getMetrics, getPending, approveCompany, rejectCompany, getAllWorkers, createValidacion,
  getAllCompanies, verifyCompany, getAllUsers, assignRole,
  getLiceoRequests, approveLiceo, rejectLiceo,
  getBadgeRequests, approveBadge, rejectBadge,
  getAdminNotifications, readAllAdminNotifications,
  bulkImport,
}
```

- [ ] **Step 2: Registrar ruta en admin.routes.js**

```js
router.put('/companies/:id/reject', auth, requireRole('ADMIN', 'TEACHER'), adminController.rejectCompany)
```

- [ ] **Step 3: Agregar `rejectCompany` en services/index.js**

```js
export const adminService = {
  // ... existentes ...
  rejectCompany: (id, feedback) => api.put(`/admin/companies/${id}/reject`, { feedback }),
  // ...
}
```

- [ ] **Step 4: Agregar botón Rechazar en AdminDashboard**

En la sección "Verificaciones de Empresa" (línea ~866), añadir botón Rechazar junto al de Verificar:
```jsx
<div style={{ display:'flex', gap:6 }}>
  <button
    onClick={() => setConfirmModal({
      label:       'Rechazar verificación',
      targetName:  c.nombreEmpresa || c.user?.nombre,
      actionLabel: 'Rechazar',
      danger:      true,
      action: async (feedback) => {
        await adminService.rejectCompany(c.id, feedback)
        setCompanies(r => r.filter(x => x.id !== c.id))
        toast.success('Verificación rechazada')
      },
    })}
    style={{ padding:'4px 12px', borderRadius:7, border:'1px solid rgba(239,68,68,.3)', background:'rgba(239,68,68,.08)', color:'#ef4444', fontSize:'.72rem', fontWeight:500, cursor:'pointer' }}
  >
    ✕ Rechazar
  </button>
  <button
    onClick={() => setConfirmModal({
      label:       'Aprobar empresa',
      // ... (código existente sin cambios)
    })}
    style={{ ... }}
  >
    ✔ Verificar
  </button>
</div>
```

- [ ] **Step 5: Commit**
```bash
git add backend/src/controllers/admin.controller.js backend/src/routes/admin.routes.js frontend/src/pages/admin/AdminDashboard.jsx frontend/src/services/index.js
git commit -m "feat: add reject company verification in admin with feedback modal"
```

---

### Task 7: Fix BOM en CSV bulk import

**Files:**
- Modify: `backend/src/controllers/admin.controller.js` (función `bulkImport`, ~línea 267)

- [ ] **Step 1: Strip BOM antes de parsear**

En `bulkImport`, reemplazar la línea donde se convierte el buffer a texto:
```js
// ANTES (línea ~267):
const text  = buf.toString('utf-8')

// DESPUÉS:
const text  = buf.toString('utf-8').replace(/^\uFEFF/, '')
```

- [ ] **Step 2: Commit**
```bash
git add backend/src/controllers/admin.controller.js
git commit -m "fix: strip UTF-8 BOM from CSV before parsing to prevent skipped rows"
```

---

### Task 8: Fix bloque blanco al colapsar secciones en AdminDashboard

**Files:**
- Modify: `frontend/src/pages/admin/AdminDashboard.jsx`

- [ ] **Step 1: Agregar `alignItems: 'flex-start'` a todos los divs `.admin-g2`**

En `AdminDashboard.jsx` hay 3 divs con `className="admin-g2"` (líneas ~788, ~852, ~863). Agregar `style={{ alignItems: 'flex-start' }}` a cada uno:

```jsx
// Línea ~788:
<div className="admin-g2" style={{ alignItems: 'flex-start' }}>

// Línea ~852:
<div className="admin-g2" style={{ alignItems: 'flex-start' }}>

// Línea ~863:
<div className="admin-g2" style={{ alignItems: 'flex-start' }}>
```

- [ ] **Step 2: Commit**
```bash
git add frontend/src/pages/admin/AdminDashboard.jsx
git commit -m "fix: add align-items flex-start to admin grid to prevent white block on section collapse"
```

---

## Self-Review

- **Spec coverage:** Todos los 7 bugs cubiertos en Tasks 1-8 (task 3+4 cubren el bug de postulación, 5+6 cubren candidatos+contactar).
- **Placeholders:** Ninguno — todo el código es concreto.
- **Type consistency:** `getPostulaciones` definido en controller, registrado en routes, y consumido en services con el mismo nombre.
